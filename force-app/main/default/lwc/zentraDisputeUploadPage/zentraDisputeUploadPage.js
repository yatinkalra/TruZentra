import { LightningElement } from 'lwc';

export default class ZentraDisputeUploadPage extends LightningElement {
    // Pure header + footer component — no logic needed.
    // Place the Flow component separately on the page between header and footer.
}